import React from "react"

const TextField = props => {
  return (
    <TextField
      {...props}
      color="primary"
      variant="outlined"
      margin="dense"
      fullWidth
    />
  )
}

export default TextField
